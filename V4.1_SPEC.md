# SaaS 创意验证分析管道 V4.1 — 完整开发规格文档

> **目标读者**: Codex / Claude Code / 任何AI编码代理
> **目的**: 从零构建一套完整的、零AI成本的、纯Python规则引擎驱动的需求管道系统
> **作者**: 主人 & Hermes

---

## 一、系统概述

### 1.1 这是什么

一个**纯Python**的自动化管道，用于：

1. **采集信号** — 从 Reddit RSS 实时抓取独立开发者社区的需求/痛点帖子
2. **前置筛选** — 用5条红线一票否决不靠谱的想法
3. **五维评分** — 量化每个机会的质量
4. **评级分类** — 🟢 GREEN / 🟡 YELLOW / 🔴 RED
5. **输出报告** — 固定格式的 Markdown 报告，可直接决策

### 1.2 核心约束（铁律）

| 约束 | 说明 |
|------|------|
| **零AI成本** | 全程不调用任何 AI API（OpenAI / Claude / 任何 LLM） |
| **个人可落地** | 一个人能独立完成的项目 |
| **无AI依赖** | 项目本身不能依赖AI才能完成核心功能 |
| **必须有市场** | 没有需求 = 没有市场 = 没有付费意愿 |
| **全球市场** | 目光放在海外，国内付费意愿低不作为主要目标 |

### 1.3 技术栈

- **语言**: Python 3.10+
- **RSS解析**: `feedparser`
- **网络请求**: `urllib`（标准库）
- **无外部数据库**: 所有结果存为 JSON 文件
- **依赖最小化**: `pip install feedparser` 是唯一的第三方依赖

---

## 二、数据来源

### 2.1 Reddit RSS

Reddit JSON API 会返回 403，**必须用 RSS**。

```
URL格式: https://www.reddit.com/r/{subreddit}/.rss
```

### 2.2 扫描的子版块（12个）

```python
SUBREDDITS = [
    # 核心：独立开发者 + SaaS
    "indiehackers",
    "microsaas",
    "SaaS",
    "EntrepreneurRideAlong",
    "SideProject",
    # 辅助：更广泛的创业 + 技术
    "solopreneur",
    "freelance",
    "webdev",
    "nocode",
    "smallbusiness",
    "apps",
    "webhosting",
]
```

### 2.3 每个子版块抓取

- 最新 **25** 篇帖子
- 必须有 `feedparser`
- RSS 可能偶尔超时，需要异常处理（超时标记为"RSS Timeout"）

---

## 三、前置筛选 — 5条红线（一票否决）

**任何一条红线触发 → 直接标记 🔴 RED，不进入评分。**

### 红线1: 巨头压制

帖子讨论的领域已有不可逾越的巨头玩家。

```python
GIANT_KEYWORDS = [
    "google", "microsoft", "amazon", "apple", "meta", "facebook",
    "salesforce", "hubspot", "shopify", "slack", "zoom", "aws",
    "openai", "anthropic", "deepseek", "google ai",
    "zendesk", "intercom", "atlassian", "notion", "figma",
]
```

**判断逻辑**: 关键词命中 → 触发。但注意：如果帖子是在**抱怨**巨头的产品太贵/不好用，那是**机会信号**而非红线。需要区分"讨论巨头"和"被巨头压制"。

**改进逻辑**:
- 如果帖子标题/内容包含"替代"、"alternative"、"instead of"、"switch from" + 巨头关键词 → 这是**机会**，不触发红线
- 如果帖子标题/内容只是纯讨论巨头本身 → 触发红线

### 红线2: 平台合规

帖子讨论的是需要官方API/认证的领域，且绕过成本高。

```python
PLATFORM_KEYWORDS = [
    "facebook api", "google api", "tiktok api", "instagram api",
    "youtube api", "linkedin api", "reddit api",
    "pci compliance", "hipaa compliance", "sox compliance",
]
```

**判断逻辑**: 关键词命中 + 帖子标题/内容提到"合规"、"certification"、"license" → 触发。

### 红线3: 技术泥潭

帖子讨论的技术领域复杂度远超个人能力。

```python
TECH_KEYWORDS = [
    "blockchain", "defi", "web3", "smart contract", "cryptocurrency",
    "machine learning model", "train a model", "computer vision",
    "self-hosted llm", "fine-tune", "medical device", "iot device",
    "robotics", "hardware", "firmware",
]
```

**判断逻辑**: 关键词命中 → 触发。

### 红线4: 护城河缺失

帖子讨论的领域已有免费开源替代品，或功能过于通用。

```python
MOAT_KEYWORDS = [
    "open source alternative", "free alternative", "oss",
    "github", "gitlab", "wordpress plugin", "chrome extension",
    "bookmarklet", "greasemonkey", "tampermonkey",
    "todo list", "note taking app", "calculator",
    "password manager", "clipboard manager",
]
```

**判断逻辑**: 关键词命中 → 触发。但注意：如果帖子是在**找免费替代品但找不到好用的**，那是**机会信号**。

**改进逻辑**:
- 如果帖子标题/内容包含"looking for"、"anyone know"、"recommend" + 免费替代关键词 → 这是**需求**，不触发红线
- 如果帖子是在说"我做了一个开源替代品" → 触发红线（护城河缺失）

### 红线5: 假性需求

帖子没有表达真实痛点，或痛点无法转化为付费产品。

```python
FAKE_KEYWORDS = [
    "what do you think", "brainstorm", "idea validation",
    "general discussion", "opinion", "poll",
    "just wondering", "curious", "what if",
]
FAKE_PATTERNS = [
    r"^(what|how|why|when|where)\s",  # 纯提问无痛点
    r"^(does anyone|anyone know|can someone)",  # 泛泛提问
]
```

**判断逻辑**: 关键词/正则命中 → 触发。但注意：如果帖子包含**痛点信号**（"frustrated"、"hate"、"waste of time"）或**付费信号**（"pay for"、"willing to"）→ 不触发红线。

---

## 四、帖子分类

通过关键词将帖子分类为不同类型，不同类型进入不同的评分逻辑。

```python
CATEGORIES = {
    "pain_point": {
        "keywords": [
            "frustrated", "annoyed", "hate", "wish there was",
            "problem", "issue", "struggle", "difficult", "pain",
            "broken", "terrible", "waste of time", "sucks",
            "wish", "need", "looking for", "can't find",
            "finally found", "solution", "fix",
        ],
        "description": "用户明确表达痛点"
    },
    "willingness_to_pay": {
        "keywords": [
            "pay for", "willing to pay", "shut up and take my money",
            "take my money", "how much", "pricing", "subscription",
            "worth it", "budget", "cost", "expensive", "cheap",
            "discount", "lifetime deal", "ltd",
        ],
        "description": "用户表达付费意愿"
    },
    "idea_request": {
        "keywords": [
            "idea", "suggest", "recommend", "what should i build",
            "what to build", "saas idea", "micro saas",
            "side project idea", "business idea", "niche",
        ],
        "description": "用户在寻求创意方向"
    },
    "feedback_request": {
        "keywords": [
            "feedback", "review", "critique", "thoughts",
            "what do you think", "rate", "evaluate",
            "landing page", "mvp", "beta", "early access",
        ],
        "description": "用户在请求反馈"
    },
    "competitor_complaint": {
        "keywords": [
            "alternative to", "switch from", "instead of",
            "competitor", "replace", "migration", "moving from",
            "fed up with", "done with", "leaving",
        ],
        "description": "用户对竞品不满"
    },
}
```

**分类规则**: 遍历所有分类，统计命中关键词数，取最高的分类。如果都没有命中，默认为 `pain_point`。

---

## 五、五维评分体系（各20%）

### 5.1 维度定义

| 维度 | 权重 | 含义 |
|------|------|------|
| **痛点强度** | 20% | 需求是否真实、强烈 |
| **开发性价比** | 20% | 以个人开发者的技术栈，能否用最小成本实现 |
| **生存稳定性** | 20% | 领域是否受巨头/政策/市场波动影响 |
| **获客阻力** | 20% | 能否以低成本获取早期用户 |
| **变现确定性** | 20% | 付费路径是否清晰 |

### 5.2 评分规则

每个维度评分 **0-10**，总分 = 五维加权和（满分10分）。

#### 维度1: 痛点强度

```python
def calc_pain_score(opportunity):
    score = 5.0  # 基线
    title_lower = opportunity["title"].lower()
    content_lower = opportunity.get("content", "").lower()
    text = f"{title_lower} {content_lower}"

    # 强痛点关键词（+2分）
    strong_pain = [
        "frustrated", "annoyed", "hate", "terrible", "sucks",
        "waste of time", "pain", "nightmare", "broken",
        "desperately", "urgent", "critical", "deadline",
    ]
    for kw in strong_pain:
        if kw in text:
            score += 0.5
            if score >= 10:
                break

    # 弱痛点关键词（+1分）
    weak_pain = [
        "wish", "need", "looking for", "problem", "issue",
        "struggle", "difficult", "hard to", "can't find",
    ]
    for kw in weak_pain:
        if kw in text:
            score += 0.3
            if score >= 10:
                break

    # 付费意愿关键词（额外+1分）
    pay_keywords = [
        "pay for", "willing to pay", "take my money",
        "worth it", "budget", "pricing",
    ]
    for kw in pay_keywords:
        if kw in text:
            score += 0.5
            if score >= 10:
                break

    # 发帖回复数越多，说明话题越热
    comments = opportunity.get("comments", 0)
    if comments > 50:
        score += 1.0
    elif comments > 20:
        score += 0.5
    elif comments > 10:
        score += 0.3

    return min(score, 10.0)
```

#### 维度2: 开发性价比

```python
def calc_dev_score(opportunity):
    score = 5.0
    title_lower = opportunity["title"].lower()
    content_lower = opportunity.get("content", "").lower()
    text = f"{title_lower} {content_lower}"

    # 个人能做的关键词（+2分）
    solo_friendly = [
        "solo", "one person", "indie", "side project",
        "weekend", "prototype", "mvp", "no-code", "low-code",
        "browser extension", "chrome extension", "api",
        "saas", "landing page", "dashboard", "cron job",
        "script", "automation", "tool", "utility",
        "cli", "web app", "static site", "shopify app",
        "wordpress plugin", "notion template",
    ]
    for kw in solo_friendly:
        if kw in text:
            score += 0.4
            if score >= 10:
                break

    # 需要团队的关键词（-3分）
    team_needed = [
        "team", "hiring", "co-founder", "partnership",
        "enterprise", "b2b sales", "compliance",
        "hardware", "manufacturing", "inventory",
    ]
    for kw in team_needed:
        if kw in text:
            score -= 0.8
            if score < 0:
                break

    # 技术栈简单性加分
    simple_tech = [
        "python", "javascript", "html", "css", "react",
        "next.js", "node", "sqlite", "postgres", "firebase",
        "supabase", "vercel", "netlify", "stripe",
    ]
    for kw in simple_tech:
        if kw in text:
            score += 0.3
            if score >= 10:
                break

    return max(min(score, 10.0), 0.0)
```

#### 维度3: 生存稳定性

```python
def calc_survival_score(opportunity):
    score = 7.0  # 基线较高
    title_lower = opportunity["title"].lower()
    content_lower = opportunity.get("content", "").lower()
    text = f"{title_lower} {content_lower}"

    # 风险关键词（-2分）
    risk_keywords = [
        "api change", "policy update", "terms of service",
        "tos", "compliance", "regulation", "legal",
        "ban", "suspend", "deplatform", "crackdown",
        "apple", "google play", "app store", "steam",
    ]
    for kw in risk_keywords:
        if kw in text:
            score -= 0.5
            if score < 0:
                break

    # 稳定性加分（+1分）
    stable_keywords = [
        "recurring", "subscription", "monthly", "annual",
        "long term", "sustainable", "passive income",
        "retention", "sticky", "essential",
    ]
    for kw in stable_keywords:
        if kw in text:
            score += 0.3
            if score >= 10:
                break

    return max(min(score, 10.0), 0.0)
```

#### 维度4: 获客阻力

```python
def calc_growth_score(opportunity):
    score = 5.0
    title_lower = opportunity["title"].lower()
    content_lower = opportunity.get("content", "").lower()
    text = f"{title_lower} {content_lower}"

    # 低成本获客关键词（+2分）
    easy_growth = [
        "reddit", "twitter", "hacker news", "product hunt",
        "indie hackers", "community", "word of mouth",
        "organic", "seo", "blog", "content marketing",
        "free tier", "freemium", "viral", "referral",
        "open source", "github stars",
    ]
    for kw in easy_growth:
        if kw in text:
            score += 0.4
            if score >= 10:
                break

    # 高成本获客关键词（-2分）
    hard_growth = [
        "ads", "advertising", "paid marketing", "ppc",
        "cold email", "cold outreach", "sales team",
        "enterprise sales", "account executive",
    ]
    for kw in hard_growth:
        if kw in text:
            score -= 0.5
            if score < 0:
                break

    return max(min(score, 10.0), 0.0)
```

#### 维度5: 变现确定性

```python
def calc_monetization_score(opportunity):
    score = 5.0
    title_lower = opportunity["title"].lower()
    content_lower = opportunity.get("content", "").lower()
    text = f"{title_lower} {content_lower}"

    # 明确变现路径（+2分）
    clear_monetization = [
        "subscription", "monthly", "annual", "pricing",
        "pay", "revenue", "mrr", "arr", "profit",
        "lifetime deal", "one-time", "per seat", "usage based",
        "tier", "plan", "upgrade", "premium",
    ]
    for kw in clear_monetization:
        if kw in text:
            score += 0.4
            if score >= 10:
                break

    # 付费信号（+1分）
    pay_signals = [
        "willing to pay", "pay for", "take my money",
        "shut up and take", "would buy", "how much",
        "worth paying", "not free",
    ]
    for kw in pay_signals:
        if kw in text:
            score += 0.5
            if score >= 10:
                break

    # 变现模糊关键词（-1分）
    vague_monetization = [
        "free", "open source", "community", "donation",
        "sponsor", "patreon",
    ]
    for kw in vague_monetization:
        if kw in text:
            score -= 0.3
            if score < 0:
                break

    return max(min(score, 10.0), 0.0)
```

### 5.3 总分计算

```python
def calc_total_score(opportunity):
    pain = calc_pain_score(opportunity)
    dev = calc_dev_score(opportunity)
    survival = calc_survival_score(opportunity)
    growth = calc_growth_score(opportunity)
    money = calc_monetization_score(opportunity)

    total = (pain + dev + survival + growth + money) / 5.0 * 10.0
    return round(total, 1), {
        "痛点强度": round(pain, 1),
        "开发性价比": round(dev, 1),
        "生存稳定性": round(survival, 1),
        "获客阻力": round(growth, 1),
        "变现确定性": round(money, 1),
    }
```

---

## 六、评级体系

| 等级 | 条件 | 含义 |
|------|------|------|
| 🟢 **GREEN** | 总分 ≥ 40 且无红线触发 | 值得深度评估，可进入验证阶段 |
| 🟡 **YELLOW** | 总分 25-39 且无红线触发 | 有机会但需谨慎，需深度验证 |
| 🔴 **RED** | 总分 < 25 或触发任一红线 | 不可行，直接跳过 |

**注意**: 五维评分是 0-10 分制，总分也是 0-10 分制。上述阈值需要调整为 0-10 制：

```python
# 正确的评级阈值（0-10分制）
def calc_rating(total_score, red_triggered):
    if red_triggered:
        return "🔴 RED"
    if total_score >= 5.0:
        return "🟢 GREEN"
    elif total_score >= 3.0:
        return "🟡 YELLOW"
    else:
        return "🔴 RED"
```

---

## 七、报告格式

每次运行输出一份 Markdown 报告，固定格式，包含以下5个模块。

### 7.1 报告模板

```markdown
# 需求管道 V4.1 — 分析报告

**日期**: {date}
**扫描**: {subreddit_count}个子版块 × {posts_per_sub}帖 = {total_posts}个帖子
**分类出**: {categorized_count}个机会
**🟢 GREEN**: {green_count}个 | **🟡 YELLOW**: {yellow_count}个 | **🔴 RED**: {red_count}个

---

## 🟢 GREEN 机会（可直接开始）

> 以下机会通过所有红线检查，且评分 ≥ 5.0

### {rank}. {title}
- **来源**: r/{subreddit} | **评分**: {total}/10
- **链接**: {url}
- **帖子内容摘要**: {content_summary}

**生存校验**:
- [ ] 巨头压制: {pass/fail}
- [ ] 平台合规: {pass/fail}
- [ ] 技术泥潭: {pass/fail}
- [ ] 护城河缺失: {pass/fail}
- [ ] 假性需求: {pass/fail}

**五维详情**:
| 维度 | 分数 | 依据 |
|------|------|------|
| 痛点强度 | {pain}/10 | {pain_reason} |
| 开发性价比 | {dev}/10 | {dev_reason} |
| 生存稳定性 | {survival}/10 | {survival_reason} |
| 获客阻力 | {growth}/10 | {growth_reason} |
| 变现确定性 | {money}/10 | {money_reason} |

**核心洞察**: {key_insight}
**落地建议**: {action_items}

---

## 🟡 YELLOW 机会（需深度验证）

> 以下机会未触发红线，但评分在 3.0-5.0 之间

（格式同上，但可以更简洁）

---

## 🔴 RED 机会（一票否决）

> 以下机会触发了红线，不建议继续

| # | 标题 | 触发红线 | 原因 |
|---|------|---------|------|
| 1 | {title} | 红线{N}: {name} | {reason} |

---

## 📊 红线触发统计

| 红线 | 触发数 | 占比 |
|------|--------|------|
| 红线1: 巨头压制 | {count} | {pct}% |
| 红线2: 平台合规 | {count} | {pct}% |
| 红线3: 技术泥潭 | {count} | {pct}% |
| 红线4: 护城河缺失 | {count} | {pct}% |
| 红线5: 假性需求 | {count} | {pct}% |

## 💡 关键发现

- {finding_1}
- {finding_2}
- {finding_3}
```

---

## 八、项目结构

```
~/.hermes/scripts/demand-pipeline/
├── scan_v4.py              # V4.1 主入口（唯一需要运行的文件）
├── data/
│   ├── raw_posts.json      # 原始抓取的帖子
│   ├── opportunities.json  # 过滤+评分后的机会
│   └── reports/            # 历史报告
│       └── YYYY-MM-DD.md
└── V4.1_SPEC.md            # 本文档
```

---

## 九、CLI 接口

```bash
# 完整管道（推荐）
python3 ~/.hermes/scripts/demand-pipeline/scan_v4.py

# 快速扫描（跳过深度分析，只输出Top 20）
python3 ~/.hermes/scripts/demand-pipeline/scan_v4.py --quick

# 只显示特定评级
python3 ~/.hermes/scripts/demand-pipeline/scan_v4.py --rating green
python3 ~/.hermes/scripts/demand-pipeline/scan_v4.py --rating yellow

# 只分析特定子版块
python3 ~/.hermes/scripts/demand-pipeline/scan_v4.py --subreddits indiehackers,microsaas

# 自定义每版块抓取数量
python3 ~/.hermes/scripts/demand-pipeline/scan_v4.py --limit 50
```

---

## 十、Cron Job 配置

```
每日 09:00 自动运行
推送结果到当前对话
Job ID: 602f96c8e625
```

配置 prompt:
```
运行需求管道V4.1完整分析，完整展示所有🟢和🟡机会的详细评分信息，包含所有五维得分和判断依据。
扫描14个子版块，每版块25篇帖子。如发现≥5分的🟢机会，可主动提议进一步调研验证。
```

---

## 十一、实现注意事项

### 11.1 Reddit RSS 坑

- **不要用** `https://www.reddit.com/r/{sub}/.json` → 会 403
- **必须用** `https://www.reddit.com/r/{sub}/.rss`
- User-Agent 必须设置（模拟浏览器）
- 超时设为 10 秒，超时跳过该子版块
- feedparser 解析 RSS 时，`entry.summary` 可能包含 HTML，需要清理

### 11.2 评分边界情况

- 所有评分结果必须 `max(min(score, 10.0), 0.0)` 夹在 0-10 之间
- 五个维度权重相等（各20%），不要轻易改权重
- 总分 = 五维平均分（0-10），不是加权和

### 11.3 红线判断的模糊性

红线不是简单的关键词命中，需要考虑上下文：
- "looking for alternative to Teachable" → 巨头关键词命中，但这是**机会信号**
- "I built a free alternative to Notion" → 护城河关键词命中，这是**红线**
- 区分方法：看帖子是在**抱怨**还是在**提供**

### 11.4 输出到文件

- 每次运行自动保存报告到 `data/reports/YYYY-MM-DD.md`
- 原始数据保存到 `data/raw_posts.json` 和 `data/opportunities.json`
- 支持 `--quick` 模式跳过详细输出

---

## 十二、V4.1 与 V3 的区别总结

| 特性 | V3 | V4.1 |
|------|-----|------|
| 前置筛选 | 硬性过滤（个人可行性+无AI） | **5条红线一票否决** |
| 评分维度 | Solo Fit 30% + Pain 25% + Money 25% + Speed 15% + Comp 5% | **痛点/性价比/稳定性/获客/变现 各20%** |
| 评级 | A/B/C/D | **🟢 GREEN / 🟡 YELLOW / 🔴 RED** |
| 报告格式 | 简单输出 | **5个固定模块**（基础信息/生存校验/五维详情/核心洞察/落地建议） |
| 红线机制 | 无 | **5条红线，一票否决** |
| 上下文判断 | 无 | **关键词+上下文语义区分** |

---

## 十三、给 Codex 的执行建议

1. **先跑通最小版本** — 只做 RSS 采集 + 简单输出，确认 feedparser 能用
2. **再加红线** — 逐条实现5条红线，每条写单元测试
3. **再加评分** — 逐个维度实现评分函数
4. **最后加报告** — 按模板生成 Markdown
5. **测试用例** — 用以下已验证的帖子测试：
   - "绕过API的推文工具" → 应触发红线3（技术泥潭）
   - "SaaS创意验证平台" → 应为 🟡 YELLOW
   - "视频编辑工具" → 应触发红线1（巨头压制）

---

*文档版本: V4.1 | 最后更新: 2026-04-30*
